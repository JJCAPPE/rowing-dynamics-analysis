"""pytest config for the rowing test suite.

`pyproject.toml` already declares `pythonpath = ["src"]`, so the `rowing`
package is importable without manual `sys.path` patching.
"""
from __future__ import annotations
